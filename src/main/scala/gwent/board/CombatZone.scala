package cl.uchile.dcc
package gwent.board

import gwent.Player

import cl.uchile.dcc.gwent.cards.{AbstractUnitCard, Card}

/** Trait representing a combat zone in the Gwen't game.
 *
 * A combat zone is characterized by the who plays their unit cards in it.
 * This trait serves as a common interface for all 3 types of zones that exist in the gwen't game,
 * close combat zone, ranged zone and siege zone. 
 *
 * 
 *
 */
trait CombatZone {
  
}
